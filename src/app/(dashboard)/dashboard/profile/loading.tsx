import LoadingState from "@/components/ui/loadingState";
import React from "react";

const ProfileLoading = () => {
  return (
    <LoadingState
      title="Loading profile"
      description="Preparing Users Profile interafce"
    />
  );
};

export default ProfileLoading;
